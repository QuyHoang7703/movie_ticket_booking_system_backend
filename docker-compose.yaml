
services:
  server:
    build:
      context: .
      target: development
    ports:
      - 8081:8080
      - 8000:8000
    depends_on:
      db:
        condition: service_healthy
    environment:
      - MYSQL_URL=jdbc:mysql://db:3306/bookingmovie
    develop:
      watch:
        - action: rebuild
          path: .
  db:
    image: mysql
    restart: always
    volumes:
      - mysql-db-data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=admin123
      - MYSQL_DATABASE=bookingmovie
      - MYSQL_USER=bookingmovie
      - MYSQL_PASSWORD=bookingmovie
    ports:
      - 3307:3306
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "bookingmovie", "--password=bookingmovie"]
      interval: 10s
      timeout: 5s
      retries: 5
volumes:
  mysql-db-data:


